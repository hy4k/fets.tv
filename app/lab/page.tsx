import { AppShell } from '@/app/layout';
const seats = Array.from({ length: 24 }, (_, i) => i === 3 ? 'free' : i === 6 ? 'assign' : i === 9 ? 'warn' : 'active');
export default function LabPage() { return <AppShell active="lab"><div className="page"><div className="page-head"><div /><div className="head-right"><span className="tiny">24 seats · 18 active</span></div></div><div className="section"><div className="lab-tabs"><button className="active">LAB A</button><button>LAB B</button><button>LAB C</button></div><div className="seat-grid">{seats.map((state, i) => <div className={`seat ${state}`} key={i}><b>A-{String(i + 1).padStart(2, '0')}</b><span>{state === 'free' ? 'FREE' : state === 'assign' ? 'FETS-021' : state === 'warn' ? 'FETS-020' : '● ACTIVE'}</span></div>)}</div><div className="legend"><span><i />ACTIVE</span><span><i style={{ background: '#88aaff' }} />ASSIGNED</span><span><i style={{ background: '#80776b' }} />FREE</span><span><i style={{ background: '#ff7259' }} />FAULT</span></div></div><div className="section"><div className="section-bar">Handoffs <span className="gold">4</span></div><div className="display-list">{['FETS-021 · BIOMETRICS → LAB A','FETS-022 · WAITING → FRISKING','FETS-023 · LAB B → SIGN OUT','FETS-024 · ARRIVED → ID CHECK'].map(item => <div className="display-item" key={item}>{item}<button className="btn" style={{ float: 'right', padding: '4px 7px' }}>Accept</button></div>)}</div></div></div></AppShell>; }
