'use client';

import Link from 'next/link';
import { useEffect, useMemo, useState } from 'react';
import { createBrowserSupabaseClient } from '@/lib/supabase';
import type { Candidate } from '@/types/domain';

const fallback: Candidate[] = [
  { id: 'demo-014', public_token: 'FETS-014', first_name: 'Aparna', last_name: 'Venugopal', part: 'PART II', place: 'Kozhikode', roster_number: '014', status: 'scheduled', scheduled_at: '2026-09-14T09:00:00+05:30', arrival_at: null, check_in_at: null, id_verified_at: null, locker_key: null, frisked_at: null, biometrics_at: null, workstation_id: null, lab_entry_at: null, testing_started_at: null, completed_at: null, signed_out_at: null },
  { id: 'demo-015', public_token: 'FETS-015', first_name: 'Mohammed', last_name: 'Rashid', part: 'PART I', place: 'Malappuram', roster_number: '015', status: 'scheduled', scheduled_at: '2026-09-14T09:00:00+05:30', arrival_at: null, check_in_at: null, id_verified_at: null, locker_key: null, frisked_at: null, biometrics_at: null, workstation_id: null, lab_entry_at: null, testing_started_at: null, completed_at: null, signed_out_at: null },
  { id: 'demo-016', public_token: 'FETS-016', first_name: 'Nisha', last_name: 'Balakrishnan', part: 'PART II', place: 'Kozhikode', roster_number: '016', status: 'id_checked', scheduled_at: '2026-09-14T09:30:00+05:30', arrival_at: '2026-09-14T09:18:00+05:30', check_in_at: '2026-09-14T09:20:00+05:30', id_verified_at: '2026-09-14T09:20:00+05:30', locker_key: 'K-16', frisked_at: null, biometrics_at: null, workstation_id: null, lab_entry_at: null, testing_started_at: null, completed_at: null, signed_out_at: null },
];

export function FrontOffice() {
  const supabase = createBrowserSupabaseClient();
  const [candidates, setCandidates] = useState<Candidate[]>(fallback);
  const [selected, setSelected] = useState<Candidate>(fallback[0]);
  const [filter, setFilter] = useState('');
  const [message, setMessage] = useState('');
  const [loading, setLoading] = useState(true);

  useEffect(() => {
    let alive = true;
    supabase.from('candidates').select('*').order('scheduled_at').limit(100).then(({ data, error }) => { if (!alive) return; setLoading(false); if (error) setMessage(error.message); if (data?.length) { setCandidates(data as Candidate[]); setSelected(data[0] as Candidate); } });
    const channel = supabase.channel('front-office-candidates').on('postgres_changes', { event: '*', schema: 'public', table: 'candidates' }, payload => { const next = payload.new as Candidate; setCandidates(current => current.some(item => item.id === next.id) ? current.map(item => item.id === next.id ? next : item) : [next, ...current]); setSelected(current => current.id === next.id ? next : current); }).subscribe();
    return () => { alive = false; supabase.removeChannel(channel); };
  }, []);

  const visible = useMemo(() => candidates.filter(candidate => (candidate.first_name + ' ' + candidate.last_name + ' ' + candidate.public_token + ' ' + (candidate.place ?? '')).toLowerCase().includes(filter.toLowerCase())), [candidates, filter]);
  const isDemoRow = selected.id.startsWith('demo-');
  const patchSelected = (patch: Partial<Candidate>) => { const next = { ...selected, ...patch }; setSelected(next); setCandidates(current => current.map(item => item.id === next.id ? next : item)); };
  async function transition(to: Candidate['status'], note?: string, locker?: string) { if (isDemoRow) { setMessage('This is preview data. Import a roster to enable live candidate actions.'); return; } setMessage(''); const { data, error } = await supabase.rpc('transition_candidate', { p_candidate_id: selected.id, p_to_status: to, p_note: note ?? null, p_locker_key: locker ?? selected.locker_key }); if (error) { setMessage(error.message); return; } if (data) patchSelected(data as Candidate); }
  async function verifyId() { await transition('id_checked', 'ID cross-verified at Front Office'); }
  async function issueKey() { if (!selected.id_verified_at && selected.status === 'scheduled') await verifyId(); const key = selected.locker_key ?? 'K-' + selected.public_token.slice(-2); await transition('waiting', 'Locker key issued', key); }
  async function completeCheckIn() { if (isDemoRow) { setMessage('This preview row is read-only. Import a roster to check in candidates.'); return; } if (!selected.id_verified_at) { setMessage('Verify the original ID first.'); return; } if (!selected.locker_key) { setMessage('Assign a locker key first.'); return; } await transition('waiting', 'Candidate checked in on roster'); }

  const initials = (selected.first_name[0] ?? '') + (selected.last_name[0] ?? '');
  return <div className="page"><div className="page-head"><div><div className="eyebrow">FRONT OFFICE</div><h1 className="page-title">Candidate check-in</h1></div><div className="head-right"><span className="pill green">● LIVE</span><Link className="head-btn" href="/roster">Open roster</Link></div></div><div className="two-col"><div className="panel pad"><div className="subhead" style={{ margin: '-20px -20px 16px' }}>Candidate list</div><input className="search" value={filter} onChange={event => setFilter(event.target.value)} placeholder="Search name, roster number, or token" /><div className="list">{visible.map(candidate => <button type="button" className={'candidate ' + (candidate.id === selected.id ? 'sel' : '')} key={candidate.id} onClick={() => { setSelected(candidate); setMessage(''); }}><span className="person"><span className="initials">{candidate.first_name[0]}{candidate.last_name[0]}</span><span><strong>{candidate.first_name} {candidate.last_name}</strong><small>{candidate.public_token} · {candidate.part ?? '—'} · {candidate.place ?? '—'}</small></span></span><span className={'pill ' + (candidate.status === 'scheduled' ? 'gold' : 'green')}>{candidate.status.replace('_', ' ')}</span></button>)}</div>{loading && <div className="small" style={{ marginTop: 14 }}>Loading live roster…</div>}{!loading && isDemoRow && <div className="demo-banner" role="status">Preview records are shown until a live roster is imported. Actions are disabled for preview records.</div>}</div><div className="panel detail"><div className="detail-top"><div className="person-large"><div className="initials large">{initials}</div><div><div className="name">{selected.first_name} {selected.last_name}</div><div className="meta">{selected.public_token} · {selected.part ?? '—'} · {selected.place ?? '—'}</div></div></div><div className="token">{selected.public_token}<small>PUBLIC TOKEN</small></div></div><div className="verification-list"><div className={'verification-row ' + (selected.id_verified_at ? 'done' : '')}><span className="verification-icon">{selected.id_verified_at ? '✓' : '1'}</span><div className="verification-copy"><strong>ID verification</strong><small>{selected.id_verified_at ? 'Original ID cross-verified' : 'Required before check-in'}</small></div><button type="button" className="row-action" disabled={isDemoRow || !!selected.id_verified_at} onClick={verifyId}>{selected.id_verified_at ? 'Verified' : 'Verify ID'}</button></div><div className={'verification-row ' + (selected.locker_key ? 'done' : '')}><span className="verification-icon">{selected.locker_key ? '✓' : '2'}</span><div className="verification-copy"><strong>Locker key</strong><small>{selected.locker_key ? 'Assigned key ' + selected.locker_key : 'Assign before roster check-in'}</small></div><button type="button" className="row-action" disabled={isDemoRow} onClick={issueKey}>{selected.locker_key ?? 'Assign key'}</button></div><div className={'verification-row ' + (selected.status === 'waiting' ? 'done' : '')}><span className="verification-icon">{selected.status === 'waiting' ? '✓' : '3'}</span><div className="verification-copy"><strong>Checked in on roster</strong><small>{selected.status === 'waiting' ? 'Candidate is in the waiting queue' : 'Complete ID and locker steps first'}</small></div><button type="button" className="row-action" disabled={isDemoRow || !selected.id_verified_at || !selected.locker_key || selected.status === 'waiting'} onClick={completeCheckIn}>{selected.status === 'waiting' ? 'Checked in' : 'Check in'}</button></div></div><button type="button" className={'complete ' + (selected.id_verified_at && selected.locker_key && !isDemoRow ? 'ready' : '')} disabled={isDemoRow} onClick={completeCheckIn}>{isDemoRow ? 'Preview record · import roster to activate' : selected.status === 'waiting' ? 'Checked in · send to Admin Room' : 'Complete check-in'}</button>{message && <div className="error" role="status" style={{ marginTop: 12 }}>{message}</div>}</div></div></div>;
}
